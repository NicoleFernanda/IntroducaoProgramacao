public class testes {
    public static void main(String[] args) {
        //uso para testes de cogigo
        
        //Uni5Exe01
            /*for (int n = 1; n >=1 && n <= 20; n++) {
            if (n % 2 == 0) {
                System.out.println(n + " par");
            } else {
                System.out.println(n + " impar");
            }
            } 

            //leitura dinamica
            //for (numero = 1; numero <= 20, numero++) {
            */

        
        
        
        
        //Uni5Exe02
        
            /*
            denominador = 2;
            auxiliar = 4;
            denominador = denominador + auxiliar;
            auxiliar = auxiliar + 2

            1 - auxiliar = 2
            denominador = 2 + 4
            2 - auxiliar = 6;
            denominador = 6 + 6;
            */ 






        //Uni5Exe04
            //divisor = soma 4, soma 6, soma 8, soma 10,tbc
            //2*2, 2*3, 2*4, 2*5

    
            



        //Uni5Exe05
            /*a = Math.pow(2, potencia);
            num = (int) a;
            potencia++;
            System.out.println(a);
            /*num = num + 2;
            System.out.println(num);
            a = Math.pow(num, potencia);
            num = (int)a;
            System.out.println(num);
            potencia++;*/






        //Uni5Exe07
            //if e else
            //maior numero e menor numero fora
            //comparar um numero de cada vez




        //Uni5Exe10
            /*int a = 1670;
            milhar = a / 1000;
            restom = a % 1000;

            centena = restom / 100;
            restoc = restom % 100;

            dezena = restoc / 10;
            restod = restoc % 10;

            unidade = restod;

            System.out.println(a);
            System.out.println(milhar);
            System.out.println(centena);
            System.out.println(dezena);
            System.out.println(unidade); 

            par1 = (milhar * 10) + centena;
            par2 = (dezena * 10) + unidade;
            System.out.println();
            System.out.println(par1);
            System.out.println(par2);

            soma = par1 + par2;
            mult = soma * soma;
            System.out.println(mult); */
            //SO IMPRIME 5, E AGORA?

            /*
            for (int i = 1000; i < 9999; i++) {

            int parte1 = (i / 100);
            int parte2 = (i % 100);

            int soma = parte1 + parte2;
            int potencia = (int) Math.pow(soma, 2);

            if (potencia == i) {
                System.out.println(i);
                }
            
            }
            */ 



            /*for (int i = 1000; i < 9999; i++) {

                int parte1 = (i / 100);
                int parte2 = (i % 100);
    
                int soma = parte1 + parte2;
                int potencia = (int) Math.pow(soma, 2);
    
                if (potencia == i) {
                    System.out.println(i);
                }
                
            }*/






        //Uni5Exe14
            /* int a = 20; //venda
            int b = 10; //compra
            int lucro = a - b;
            double porc = lucro / b * 100;

            System.out.println(lucro);
            System.out.println(porc); 

            Para cada mercadoria (20) ele tem o nome, o preço de compra (PC) e o preço
            de venda (PV). 
            */

            //PERGUNTAR - pq nao pede o nome da mercadoria mais que uma vez;
            //RESPOSTA - coloquei next() ao inves de nextLine();


        


        //Uni5Exe18
        /*
        if (canal == 0) {
            System.out.println("para parar, digite 1, para continuar, digite 0");
            String resposta = tec.next();
            if (resposta.equals("1")) {
                break;
            } else if (resposta.equals("0")) {
                
                continue; 
            }
        
        }*/






        //Uni5Exe20
            /*if (massa < 0.5 (gramas)) {
                break;

            } else {
                continue;
            } */

    }
    
}
